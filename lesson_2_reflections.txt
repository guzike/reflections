What happens when you initialize a repository? Why do you need to do it?

	A new ropository is created. .git directory is created.

How is the staging area different from the working directory and the repository?
What value do you think it offers?

	Staging area contains files to be commited to the repository from woring directory.

How can you use the staging area to make sure you have one commit per logical
change?

	We can add separated files to the staged area and then commit it. 

What are some situations when branches would be helpful in keeping your history
organized? How would branches help?

	Branches help when we need to create almost the same project but with some differences.

How do the diagrams help you visualize the branch structure?

	With the diagrams me can easily see the history of changes and keep the well structured project.

What is the result of merging two branches together? Why do we represent it in
the diagram the way we do?

	Result of merging is combining rows that were added in the each of branches files and that are not deleted from files since initial files versions.

What are the pros and cons of GitвЂ™s automatic merging vs. always doing merges
manually?

	Merging manually could take a lot of time, but some times we should resolve conflicts by hand.